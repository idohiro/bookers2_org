<main>

  <div class="container px-5 px-sm-0">
    <div class="row">
  <%= render 'users/list', user: @user ,users: @users , book: @book %>



    <div class="col-md-8 offset-md-1">
          <div class="col-md-8 offset-md-1">
          <h2>Book</h2>
          <table class="table table-hover table-inverse">
            <th>
              <th>
                title
              </th>
              <th>options</th>
            </tr>
             <% @books.each do |book| %>
            <tr>
              <td><%= image_tag('profile.jpg') %>
              <%= @users.name %></td>

              <td><%= @book.title %></td>
              <td><%= @book.body %></td>

            </tr>
             <% end %>
          </table>
        </div>
        </div>
      </div>
      </div>
</main>